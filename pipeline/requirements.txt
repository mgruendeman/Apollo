# Python 3.10 or 3.11 (torch 2.0 has no builds for 3.12+).
# Install torch first from PyTorch's index (see README), then: pip install -r pipeline/requirements.txt
deepfilternet==0.5.6
numpy<2
pillow>=10
soundfile>=0.12
